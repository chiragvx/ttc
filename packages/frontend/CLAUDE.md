# packages/frontend — placeholder (Phase 1)

React + TypeScript + Vite, three.js via react-three-fiber. **Read-only viewport** (protects script
integrity — no direct mesh editing). GPU morph-target slider preview; picking/HUD bind to persistent
feature tags, NOT OCCT sub-shape indices (depends on Spike 1). Do not build until Phase 1 — empty for now.
